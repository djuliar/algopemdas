# **Pengenalan HTML Dasar**

## 🧠 Apa Itu HTML?

👉 **HTML** adalah singkatan dari **HyperText Markup Language**.
HTML adalah **bahasa dasar untuk membuat halaman web**.

> Dengan HTML, kita bisa membuat struktur halaman web — seperti teks, gambar, tombol, tabel, dan tautan (link).

💡 **HTML bukan bahasa pemrograman**, tapi **bahasa markup**, artinya:

* HTML **menandai (mark)** bagian-bagian dari sebuah halaman.
* Komputer (browser) membaca tanda-tanda itu dan menampilkannya di layar.

---

## 🌐 Contoh Sederhana

Bayangkan kamu membuka sebuah halaman web yang menampilkan tulisan:

> Halo Dunia!

Ternyata di balik layar, tampilannya dibuat dengan kode seperti ini:

```html
<!DOCTYPE html>
<html>
<head>
  <title>Halaman Pertama Saya</title>
</head>
<body>
  <h1>Halo Dunia!</h1>
  <p>Selamat datang di halaman web pertama saya.</p>
</body>
</html>
```

## 🧾 Hasil di browser:

### 💡 Penjelasan Bagian-bagiannya:

| Bagian            | Penjelasan                                             |
| ----------------- | ------------------------------------------------------ |
| `<!DOCTYPE html>` | Memberi tahu browser bahwa ini adalah dokumen HTML5    |
| `<html>`          | Awal dari dokumen HTML                                 |
| `<head>`          | Bagian kepala (berisi informasi seperti judul halaman) |
| `<title>`         | Menentukan judul di tab browser                        |
| `<body>`          | Bagian isi (semua yang akan tampil di layar pengguna)  |
| `<h1>`            | Judul besar (heading 1)                                |
| `<p>`             | Paragraf teks                                          |

---

## 🧩 Struktur Dasar Dokumen HTML

```
<!DOCTYPE html>
<html>
<head>
    <title>Judul Halaman</title>
</head>
<body>
    <!-- Isi konten website -->
</body>
</html>
```

Semua halaman web **harus punya struktur dasar ini**.
Bagian `<body>` adalah tempat kamu menulis isi konten web.

---

## 🧱 Elemen dan Tag HTML

HTML dibangun dari **elemen-elemen** yang ditulis dengan **tag**.
Tag biasanya terdiri dari **tag pembuka** dan **tag penutup**.

Contoh:

```html
<p>Ini adalah paragraf.</p>
```

| Bagian                 | Keterangan      |
| ---------------------- | --------------- |
| `<p>`                  | Tag pembuka     |
| `</p>`                 | Tag penutup     |
| `Ini adalah paragraf.` | Isi dari elemen |

---

## ✨ Beberapa Tag Dasar yang Perlu Diketahui

| Tag                  | Fungsi                                                           |
| -------------------- | ---------------------------------------------------------------- |
| `<h1>` sampai `<h6>` | Judul atau heading, dari yang terbesar (`h1`) ke terkecil (`h6`) |
| `<p>`                | Paragraf                                                         |
| `<br>`               | Ganti baris                                                      |
| `<b>`                | Teks tebal (bold)                                                |
| `<i>`                | Teks miring (italic)                                             |
| `<u>`                | Garis bawah                                                      |
| `<hr>`               | Garis pemisah horizontal                                         |
| `<a href="">`        | Membuat tautan (link)                                            |
| `<img src="">`       | Menampilkan gambar                                               |
| `<ul>` dan `<li>`    | Daftar tak berurutan (bullet list)                               |
| `<ol>` dan `<li>`    | Daftar berurutan (nomor)                                         |
| `<table>`            | Tabel                                                            |

---

## 📄 Contoh Menampilkan Teks dan Gambar

```html
<!DOCTYPE html>
<html>
<head>
    <title>Contoh HTML Dasar</title>
</head>
<body>
    <h1>Selamat Datang di Website Saya</h1>
    <p>Ini adalah paragraf pertama saya.</p>
    <img src="gambar.jpg" width="200" alt="Foto Saya">
</body>
</html>
```

🧾 Hasil:

* Tulisan “Selamat Datang di Website Saya” muncul besar di atas.
* Paragraf di bawahnya muncul sebagai teks.
* Gambar berukuran 200 piksel muncul di bawahnya.

---

## 🔗 Contoh Membuat Link (Tautan)

```html
<a href="https://www.google.com">Kunjungi Google</a>
```

🧾 Hasil:
👉 [Kunjungi Google](https://www.google.com)

Tag `<a>` disebut **anchor**, dan atribut `href` berisi alamat tujuan.

---

## 📋 Contoh Membuat Daftar (List)

### 🔸 Daftar Tak Berurutan (bullet)

```html
<ul>
  <li>HTML</li>
  <li>CSS</li>
  <li>JavaScript</li>
</ul>
```

🧾 Hasil:

* HTML
* CSS
* JavaScript

### 🔹 Daftar Berurutan (angka)

```html
<ol>
  <li>Belajar HTML</li>
  <li>Belajar CSS</li>
  <li>Belajar PHP</li>
</ol>
```

🧾 Hasil:

1. Belajar HTML
2. Belajar CSS
3. Belajar PHP

---

## 🧮 Contoh Membuat Tabel

```html
<table border="1">
  <tr>
    <th>Nama</th>
    <th>Umur</th>
  </tr>
  <tr>
    <td>Andi</td>
    <td>20</td>
  </tr>
  <tr>
    <td>Budi</td>
    <td>21</td>
  </tr>
</table>
```

🧾 Hasil:

| Nama | Umur |
| ---- | ---- |
| Andi | 20   |
| Budi | 21   |

---

## 🧠 Atribut dalam HTML

Tag HTML bisa memiliki **atribut** — yaitu **informasi tambahan** untuk mengatur tampilan atau perilaku elemen.

Contoh:

```html
<img src="gambar.jpg" width="300" alt="Foto Profil">
```

| Atribut | Fungsi                                   |
| ------- | ---------------------------------------- |
| `src`   | Lokasi gambar                            |
| `width` | Lebar gambar                             |
| `alt`   | Teks alternatif jika gambar tidak muncul |

---

## ⚙️ Cara Membuat dan Menjalankan File HTML

1️⃣ Buka aplikasi teks editor (Notepad, VS Code, Sublime, dll).
2️⃣ Tulis kode HTML kamu.
3️⃣ Simpan dengan nama:

```
index.html
```

4️⃣ Buka file itu di browser (klik dua kali, atau drag ke Chrome/Edge).

🎉 Maka halaman web buatanmu akan muncul!

---

## 🧾 Struktur Web Sederhana

```html
<!DOCTYPE html>
<html>
<head>
  <title>Profil Mahasiswa</title>
</head>
<body>
  <h1>Profil Saya</h1>
  <p>Nama saya <b>Andi</b>, mahasiswa semester 1.</p>
  <p>Saya belajar membuat website dengan HTML.</p>
  <a href="https://www.polije.ac.id">Kunjungi Kampus Saya</a>
</body>
</html>
```

🧾 Hasil di browser:

```
Profil Saya
Nama saya Andi, mahasiswa semester 1.
Saya belajar membuat website dengan HTML.
[Kunjungi Kampus Saya]
```

---

## 📚 Kesimpulan

| Konsep  | Penjelasan                                               |
| ------- | -------------------------------------------------------- |
| HTML    | Bahasa untuk membuat struktur halaman web                |
| Tag     | Tanda pembuka dan penutup elemen                         |
| Elemen  | Bagian dari halaman web, seperti teks, gambar, atau link |
| Atribut | Informasi tambahan untuk elemen HTML                     |
| Browser | Tempat menampilkan hasil HTML                            |

---

## ✍️ Latihan Sederhana

1️⃣ Buat halaman HTML bernama **profil.html** yang berisi:

* Nama kamu
* Paragraf tentang hobi kamu
* Foto kamu (boleh gambar bebas)

2️⃣ Buat halaman HTML bernama **daftar.html** yang menampilkan daftar 3 makanan favoritmu.

3️⃣ Buat link dari halaman **profil.html** ke **daftar.html**.

---

## 🎨 Analogi Sederhana

Bayangkan:

* **HTML** = kerangka atau struktur rumah 🏠
* **CSS** = cat dan desain interior 🎨
* **JavaScript** = fungsi-fungsi rumah (lampu otomatis, pintu sensor, dll) ⚡

Jadi, tanpa HTML, **tidak akan ada bentuk dasar website** — semua website **berawal dari HTML**.

---
