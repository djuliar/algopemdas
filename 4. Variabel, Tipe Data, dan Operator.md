# **Variabel**, **Tipe Data**, dan **Operator**

## 💡 1️⃣ Apa Itu Variabel?

### 📘 Pengertian:

**Variabel** adalah **tempat untuk menyimpan data di dalam memori komputer**.
Bayangkan seperti **kotak penyimpanan** yang diberi nama agar kita bisa tahu apa isinya.

---

### 🎒 Contoh Sederhana:

Bayangkan kamu punya kotak bertuliskan **“nama”**, lalu kamu isi dengan **“Budi”**.

Di komputer:

```python
nama = "Budi"
```

Artinya:

> Buat sebuah variabel bernama `nama`, dan isinya adalah `"Budi"`.

---

### 📦 Variabel Bisa Menyimpan Berbagai Jenis Data:

* Nama seseorang → `"Budi"`
* Umur seseorang → `19`
* Nilai ujian → `87.5`
* Status → `True` (benar)

---

### ⚙️ Aturan Penulisan Variabel:

1. Tidak boleh diawali angka
   ❌ `1nama` → salah
   ✅ `nama1` → benar
2. Tidak boleh mengandung spasi
   ❌ `nama siswa` → salah
   ✅ `nama_siswa` → benar
3. Gunakan nama yang jelas artinya
   ✅ `umur`, `nilai_tugas`, `total_harga`

---

### 💻 Contoh Program

```python
nama = "Budi"
umur = 19
nilai = 87.5

print("Nama:", nama)
print("Umur:", umur)
print("Nilai:", nilai)
```

🧾 Hasil:

```
Nama: Budi
Umur: 19
Nilai: 87.5
```

---

## 🧮 2️⃣ Tipe Data

### 📘 Pengertian:

**Tipe Data** adalah **jenis nilai** yang disimpan di dalam variabel.

Bayangkan kamu punya beberapa jenis kotak:

* Kotak khusus angka bulat 🎲
* Kotak khusus tulisan ✏️
* Kotak khusus nilai benar/salah ✅❌

Setiap variabel harus tahu **tipe data** apa yang disimpan di dalamnya.

---

### 🔢 Jenis–Jenis Tipe Data Dasar

| Jenis              | Contoh             | Keterangan               |
| ------------------ | ------------------ | ------------------------ |
| **Integer (int)**  | `10`, `-3`, `100`  | Angka bulat              |
| **Float (float)**  | `3.14`, `75.5`     | Angka desimal            |
| **String (str)**   | `"Halo"`, `"Budi"` | Teks atau kalimat        |
| **Boolean (bool)** | `True`, `False`    | Nilai logika benar/salah |

---

### 💻 Contoh Program

```python
nama = "Andi"      # String
umur = 20          # Integer
tinggi = 170.5     # Float
mahasiswa = True   # Boolean

print(nama, umur, tinggi, mahasiswa)
```

🧾 Hasil:

```
Andi 20 170.5 True
```

---

### 🔍 Mengecek Tipe Data

Kita bisa pakai fungsi `type()`:

```python
print(type(nama))
print(type(umur))
print(type(tinggi))
print(type(mahasiswa))
```

🧾 Hasil:

```
<class 'str'>
<class 'int'>
<class 'float'>
<class 'bool'>
```

---

## ⚙️ 3️⃣ Operator

### 📘 Pengertian:

**Operator** adalah **simbol atau tanda** yang digunakan untuk melakukan operasi pada data.

Contohnya:

* Penjumlahan (`+`)
* Pengurangan (`-`)
* Perbandingan (`>`, `<`, `==`)
* Dan lainnya

---

### 🔢 A. Operator Aritmatika (Perhitungan)

| Operator | Arti       | Contoh   | Hasil |
| -------- | ---------- | -------- | ----- |
| `+`      | Tambah     | `5 + 2`  | `7`   |
| `-`      | Kurang     | `5 - 2`  | `3`   |
| `*`      | Kali       | `5 * 2`  | `10`  |
| `/`      | Bagi       | `5 / 2`  | `2.5` |
| `//`     | Bagi bulat | `5 // 2` | `2`   |
| `%`      | Sisa bagi  | `5 % 2`  | `1`   |
| `**`     | Pangkat    | `2 ** 3` | `8`   |

💻 Contoh:

```python
a = 5
b = 2
print(a + b)  # 7
print(a ** b) # 25
```

---

### 🧮 B. Operator Perbandingan (Membandingkan Nilai)

| Operator | Arti                  | Contoh   | Hasil  |
| -------- | --------------------- | -------- | ------ |
| `==`     | Sama dengan           | `5 == 5` | `True` |
| `!=`     | Tidak sama            | `5 != 3` | `True` |
| `>`      | Lebih besar           | `5 > 3`  | `True` |
| `<`      | Lebih kecil           | `3 < 5`  | `True` |
| `>=`     | Lebih besar atau sama | `5 >= 5` | `True` |
| `<=`     | Lebih kecil atau sama | `4 <= 5` | `True` |

💻 Contoh:

```python
x = 10
y = 5
print(x > y)   # True
print(x == y)  # False
```

---

### ⚖️ C. Operator Logika (AND, OR, NOT)

Digunakan untuk **menggabungkan kondisi logika**.

| Operator | Arti                        | Contoh           | Hasil   |
| -------- | --------------------------- | ---------------- | ------- |
| `and`    | Benar jika dua-duanya benar | `True and False` | `False` |
| `or`     | Benar jika salah satu benar | `True or False`  | `True`  |
| `not`    | Membalikkan nilai           | `not True`       | `False` |

💻 Contoh:

```python
nilai = 85
lulus = (nilai >= 70) and (nilai <= 100)
print("Lulus:", lulus)
```

🧾 Hasil:

```
Lulus: True
```

---

### 🔁 D. Operator Penugasan

Digunakan untuk **mengubah nilai variabel** dengan cara cepat.

| Operator | Arti              | Contoh   | Hasil     |
| -------- | ----------------- | -------- | --------- |
| `=`      | Menyimpan nilai   | `x = 5`  | x = 5     |
| `+=`     | Tambah dan simpan | `x += 3` | x = x + 3 |
| `-=`     | Kurang dan simpan | `x -= 2` | x = x - 2 |
| `*=`     | Kali dan simpan   | `x *= 2` | x = x * 2 |

💻 Contoh:

```python
x = 5
x += 2
print(x)  # 7
```

---

## 🎯 Kesimpulan

| Konsep        | Pengertian                                      | Contoh                        |
| ------------- | ----------------------------------------------- | ----------------------------- |
| **Variabel**  | Tempat menyimpan data                           | `nama = "Budi"`               |
| **Tipe Data** | Jenis nilai di variabel                         | `int`, `float`, `str`, `bool` |
| **Operator**  | Simbol untuk menghitung atau membandingkan data | `+`, `-`, `==`, `and`         |

---

## ✍️ Latihan Sederhana

1️⃣ Buat variabel:

```python
nama = "Andi"
umur = 19
tinggi = 170.5
```

2️⃣ Tampilkan semua nilai dengan `print()`

3️⃣ Tambahkan 1 ke umur dan tampilkan umur baru.
4️⃣ Buat logika: tampilkan `"Dewasa"` jika umur >= 18.

---

## 💬 Penutup

> 🧠 Variabel adalah “tempat penyimpanan data”.
> ⚙️ Tipe Data menentukan “jenis data”.
> ➕ Operator membantu kita “memproses dan membandingkan data”.

Kalau kamu sudah memahami tiga hal ini, kamu sudah **menguasai dasar logika pemrograman** yang akan dipakai di semua bahasa seperti Python, C, Java, dan lainnya 💪

---
