# **Struktur Kontrol Percabangan (Conditional Statement)**

## 💡 Apa Itu Struktur Kontrol Percabangan?

Bayangkan kamu sedang di persimpangan jalan 🚦
Kalau lampu **hijau**, kamu **jalan**.
Kalau **merah**, kamu **berhenti**.
Kalau **kuning**, kamu **bersiap-siap**.

Nah, cara berpikir seperti itu — **memilih tindakan berdasarkan kondisi tertentu** — adalah **percabangan** dalam pemrograman.

👉 Jadi, **percabangan** adalah **struktur logika yang membuat program bisa mengambil keputusan** berdasarkan suatu kondisi.

---

## 🧩 Mengapa Percabangan Penting?

Tanpa percabangan, program hanya akan **jalan lurus terus** — tidak bisa “berpikir”.
Dengan percabangan, program bisa:

* Menentukan apakah seseorang **lulus atau tidak** berdasarkan nilai.
* Mengecek apakah pengguna **boleh login atau tidak**.
* Menampilkan **diskon** hanya jika pembelian lebih dari Rp100.000.

💬 Intinya: percabangan membuat program menjadi **cerdas dan dinamis**.

---

## ⚙️ Bentuk Dasar Percabangan (di Python)

Ada 3 bentuk dasar percabangan:
1️⃣ `if` (jika)
2️⃣ `if ... else` (jika – selain itu)
3️⃣ `if ... elif ... else` (jika – atau jika – selain itu)

---

## 🧱 1️⃣ Percabangan Sederhana (`if`)

### 📘 Pengertian:

Digunakan **jika kita hanya ingin melakukan sesuatu ketika kondisi benar**.

### 💻 Contoh:

```python
nilai = 80

if nilai >= 70:
    print("Kamu Lulus!")
```

🧾 Hasil:

```
Kamu Lulus!
```

> Karena `nilai >= 70` bernilai **True**, maka perintah di dalam `if` dijalankan.

Jika nilai kurang dari 70, maka **tidak ada yang dijalankan**.

---

## ⚖️ 2️⃣ Percabangan dengan `else`

### 📘 Pengertian:

Digunakan **jika ada dua kemungkinan keputusan**:

* Satu kondisi benar → lakukan sesuatu
* Kalau tidak → lakukan hal lain

### 💻 Contoh:

```python
nilai = 60

if nilai >= 70:
    print("Lulus")
else:
    print("Tidak Lulus")
```

🧾 Hasil:

```
Tidak Lulus
```

> Karena `nilai >= 70` salah (False), maka bagian `else` dijalankan.

---

## 🔀 3️⃣ Percabangan Ganda (`if ... elif ... else`)

### 📘 Pengertian:

Digunakan **jika ada lebih dari dua kondisi**.

### 💻 Contoh:

```python
nilai = 85

if nilai >= 90:
    print("A")
elif nilai >= 80:
    print("B")
elif nilai >= 70:
    print("C")
else:
    print("D")
```

🧾 Hasil:

```
B
```

> Program memeriksa dari atas ke bawah, dan berhenti pada kondisi yang benar pertama kali.

---

## 💬 Penjelasan Logika Percabangan

| Nilai | Kondisi                      | Hasil |
| ----- | ---------------------------- | ----- |
| 95    | `nilai >= 90`                | A     |
| 82    | `nilai >= 80`                | B     |
| 75    | `nilai >= 70`                | C     |
| 60    | Tidak memenuhi semua kondisi | D     |

---

## 🧱 Percabangan Bersarang (Nested If)

Terkadang kita butuh **if di dalam if**, disebut **percabangan bersarang**.

### 💻 Contoh:

```python
umur = 20
status = "pelajar"

if umur >= 18:
    if status == "pelajar":
        print("Diskon 50%")
    else:
        print("Diskon 20%")
else:
    print("Diskon 10%")
```

🧾 Hasil:

```
Diskon 50%
```

---

## ⚙️ Operator yang Sering Digunakan dalam Percabangan

### 🔢 Operator Perbandingan

| Operator | Arti             | Contoh   | Hasil |
| -------- | ---------------- | -------- | ----- |
| `==`     | Sama dengan      | `5 == 5` | True  |
| `!=`     | Tidak sama       | `5 != 3` | True  |
| `>`      | Lebih besar      | `5 > 3`  | True  |
| `<`      | Lebih kecil      | `2 < 4`  | True  |
| `>=`     | Lebih besar/sama | `5 >= 5` | True  |
| `<=`     | Lebih kecil/sama | `3 <= 4` | True  |

---

### 🔗 Operator Logika

| Operator | Arti                        | Contoh                   | Hasil |
| -------- | --------------------------- | ------------------------ | ----- |
| `and`    | Benar jika dua-duanya benar | `True and False` → False |       |
| `or`     | Benar jika salah satu benar | `True or False` → True   |       |
| `not`    | Membalik nilai              | `not True` → False       |       |

💻 Contoh:

```python
nilai = 80
absen = 90

if nilai >= 70 and absen >= 80:
    print("Lulus")
else:
    print("Tidak Lulus")
```

🧾 Hasil:

```
Lulus
```

---

## 📊 Ilustrasi Percabangan

```
        +--------------------+
        | Apakah nilai >= 70?|
        +---------+----------+
                  |
          Ya      |     Tidak
        +----+    |    +-------------+
        |Lulus|   |    |Tidak Lulus  |
        +----+    |    +-------------+
```

---

## 🎯 Kesimpulan

| Konsep               | Penjelasan                                    | Contoh                 |
| -------------------- | --------------------------------------------- | ---------------------- |
| **Percabangan (if)** | Menjalankan perintah hanya jika kondisi benar | `if nilai >= 70:`      |
| **If–Else**          | Menjalankan salah satu dari dua kemungkinan   | `if ... else`          |
| **If–Elif–Else**     | Menentukan banyak kondisi                     | `if ... elif ... else` |
| **Nested If**        | If di dalam if                                | `if ...: if ...:`      |

---

## ✍️ Latihan Sederhana

1️⃣ Buat program yang meminta input nilai dari pengguna.
Jika nilai ≥ 75 → tampilkan “Lulus”, selain itu “Tidak Lulus”.

2️⃣ Buat program untuk menentukan kategori usia:

```
< 13  → Anak-anak
13–17 → Remaja
18–59 → Dewasa
>= 60 → Lansia
```

3️⃣ Buat program yang menampilkan diskon:

* Jika pelanggan “member” dan total belanja ≥ 100000 → diskon 20%
* Jika bukan member → diskon 5%

---

## 🏁 Penutup

> 🧠 **Percabangan** adalah kemampuan program untuk **memilih jalan** sesuai kondisi tertentu.
> Tanpa percabangan, program tidak bisa “berpikir” atau mengambil keputusan.
> Dengan percabangan, program menjadi **interaktif, cerdas, dan dinamis**.

---
